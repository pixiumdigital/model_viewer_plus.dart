name: example
description: A project that showcases usage of model_viewer
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  carapacik_lints: ^1.4.1
  model_viewer_plus:
    path:
      ../

flutter:
  uses-material-design: true
